# Student setup

**STATUS: not yet authored — scheduled for Stage B.**

This file is a placeholder so the repository structure is visible and reviewable. It is
not content, and it is not a summary of content that exists elsewhere.

Will contain separately verified **Windows** and **macOS** setup paths (Python, container
runtime, application startup, network access), a WSL2 evaluation for Windows, Apple Silicon
vs Intel image notes, and the week-1 environment check script. Blocked on Q-04 (container
tooling choice). Platform-specific commands stay isolated here; the lab instructions
themselves are identical on both platforms.

See `planning/progress.md` for the authoring order and the current next action.
